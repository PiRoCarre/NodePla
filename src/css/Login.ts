import { theme } from "./theme";

export const LoginCss = {
    input : {
        width: '50%',
        border: 'none',
        borderRadius: 2,
        backgroundColor: theme.dark.input.primary,
    }
}