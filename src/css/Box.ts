import { theme } from './theme';

export const BoxCss = {
    box: {
        width: 600,
        minHeight: 300,
        margin: 'auto',
        backgroundColor: theme.dark.accent,
        color: theme.dark.text,
        border: "3px solid #5C5C68",
        borderRadius: 5,
        padding: 3
    }
}